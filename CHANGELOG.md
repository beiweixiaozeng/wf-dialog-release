# WF无限调优 MCP 更新日志

## v5.9.22

### ✨ 改进
- 启动时自动强制更新：发现新版本直接下载安装并重载窗口，无需用户确认

---

## v5.9.21

### 🔧 修复
- 修复分身启动时缓存的 Windsurf 路径失效（如更新后 Electron→Windsurf 重命名），现自动清除失效缓存并重新检测

---

## v5.9.20

### 🔧 修复
- Linux 分身启动无窗口：自动检测 `/usr/bin/windsurf` 等 CLI 包装脚本，解析到真实 ELF 二进制再 spawn
- Linux 分身启动三平台独立分支（macOS `open -n`、Linux `resolveLinuxBinary`、Windows `resolveSquirrelExe`）

---

## v5.9.19

### 🔧 增强
- 机器码重置新增 `telemetry.macMachineId` 字段（32位 hex，MD5 格式）
- `telemetry.machineId` 格式从 128 位升级为 256 位（64 位 hex），对齐 VSCode 标准
- 新增平台级 ID 重置：Windows 注册表 `MachineGuid`、macOS/Linux 缓存文件清理
- Linux 分身运行状态检测修复大小写不敏感匹配

---

## v5.9.18

### ✨ 改进
- 当前账号设置面板改为卡片内滑动切换，点击 ⚙️ 丝滑翻页，不再弹出下拉面板
- ⚙️ 齿轮按钮固定在卡片右上角，正/背两面始终可见，可双向切换
- 背面新增"◂ 返回"链接，操作更直观

---

## v5.9.17

### ✨ 新功能
- 账号列表三级分组：Gmail/Yahoo、域名邮箱、过期/异常/耗尽，每组均可独立折叠
- 过期/异常/耗尽分组自动归集：试用到期、密码错误(Error)、配额耗尽账号统一沉底
- 分组折叠状态持久化，刷新后记住展开/收起状态

---

## v5.9.16

### 🔧 修复
- 新用户首次导入账号后，当前账号区域提示优化：红色醒目文字引导点击 ⇄ 图标切换登录

---

## v5.9.15

### ✨ 新功能
- **跨实例账号互斥**：主实例与分身之间共享账号占用状态，自动切换/预热/快捷键切号均排除其他分身正在使用的账号
- 手动切号时若目标账号被其他分身占用，弹窗提示「仍然切换 / 切换其他账号」
- 分身启动时自动检测并切换到未被占用的账号，开箱即隔离
- 余额查询日志：新增 OutputChannel 日志输出与面板开关

### 🔧 修复
- 修复批量刷新重入问题（上轮未完成时跳过新一轮）
- 修复手动全量刷新与定时刷新并发冲突（手动刷新期间暂停定时器）
- 修复批量刷新 jitter 间隔在连续失败时丢失的问题
- 批量刷新性能优化：间隔 10 分钟→3 分钟，批次增大，jitter 缩短
- 移除无效的「等级伪装」功能
- 修复分身启动可能打开母体的问题（Squirrel 安装器桩检测 + --new-window 标志）
- 修复分身重置机器码时误改母体 storage.json 的问题（精准定位当前实例数据目录）
- Windows 分身启动增强：扩展 Windsurf 可执行文件检测路径

---

## v5.9.14

### ✨ 新功能
- 当前账号区域新增「移除当前」按钮：一键删除当前账号并自动切换到下一个（带确认弹窗）
- 导入格式新增支持：`邮箱 密码----卡密` 复合格式，自动剥离卡密后缀只导入账号
- 导入提示完善：placeholder 补充咸鱼发货格式说明（卡号/卡密行自动识别）

### 🔧 修复
- 修复密码错误的账号导入后显示为 Free 的问题，现在正确显示为红色 ERROR 徽章
- 余额查询失败（密码错误/Token失效/网络异常）的账号在批量刷新、导入刷新、手动查询三个路径均统一标记为 Error

---

## v5.9.13

### ✨ 新功能
- 库存账号排序新增 `Min`、日配额、周配额三种维度，默认按 `min(日剩余, 周剩余)` 排序
- 库存账号新增二级分组：`Gmail / Yahoo` 与 `域名邮箱`

### 🔧 修复
- 修复配额排序默认方向错误，改为高配额优先
- 修复到期时间文字显示为灰色斜体的问题，恢复正常样式
- 修复过期账号在库存中被弱化显示的问题，保留可见可用，不自动屏蔽
- 修复排序缺少无配额数据兜底时，无数据账号被错误排到最前的问题

---

## v5.9.12

### ✨ 新功能
- 账号置顶：点击 ☆ 按钮将常用账号置顶，快速定位
- 导入优化：导入账号后只查询新导入批次的余额，不再全量刷新

### 🔧 修复
- 修复重置机器码后 installationId 被 Windsurf 关闭时覆盖的问题（改为补丁方式，重启即生效）
- 修复周限额为0%时日限额仍显示100%的误导问题
- 机器码重置日志优化：三步均显示 旧值→新值

---

## v5.9.11

### ✨ 新功能
- 支持 RefreshToken 导入：适配 Gmail OAuth 等无密码账号，粘贴 `邮箱;RefreshToken` 即可（≥100字符自动识别）
- RefreshToken 账号支持查余额、切号全链路

---

## v5.9.10

### ✨ 优化
- 导入账号后自动刷新获取账号信息，避免默认显示 free 误导用户

---

## v5.9.9

### 🎨 UI 优化
- 重新设计活动栏图标：双人剪影 + 双向箭头，视觉重心平衡
- 主界面卡片分隔：当前账号与账号列表以独立卡片呈现，视觉层次更清晰

---

## v5.9.8

### 🐛 修复
- 修复启动时状态栏显示"未登录"需要点开面板才刷新的问题（启动即读缓存更新状态栏）

---

## v5.9.7

### 🐛 修复
- 修复快捷键打开面板报 "command 'workbench.view.extension.wf-dialog' not found" 的错误（view container ID 不匹配）

---

## v5.9.6

### ✨ 新功能
- 账号列表紧凑模式：圆环指示器显示日/周额度百分比，一键切换展开/紧凑视图
- 紧凑模式下保留剩余天数标签
- 切换动画丝滑过渡（max-height + opacity transition）

---

## v5.9.5

### 🔧 修复
- 修复周额度燃尽但日额度满时插件仍显示绿色/可用的误判问题
- 修复自动切号可能换到周额度已耗尽账号的问题
- 修复卡密激活失败时无反馈显示（失败消息被状态覆盖）
- 修复状态栏颜色只看日额度不看周额度的问题

### ✨ 优化
- 统一 quota 实际可用性口径：min(日剩余, 周剩余)，有额外余额时视为可用
- 卡密激活失败时同时在面板和弹窗显示具体原因
- VSIX 打包排除敏感规则和脚本文件

---

## v5.9.4

### 🔧 修复
- 修复未激活时仍能通过命令面板/快捷键使用功能的绕过漏洞
- 修复未激活时后台自动切号、余额监控、全量刷新仍在运行的问题
- 修复非永久卡 expireTime 缺失或为 0 时被误判为有效的过期逻辑漏洞
- 云端同步缺少过期时间时优先保留本地已有有效数据，避免破坏旧用户状态

### ✨ 优化
- 卡密激活成功后自动恢复后台监控和余额刷新
- 卡密失效时前端账号列表自动清空，状态栏同步更新

---

## v5.9.3

- 版本号更新

---

## v5.9.2

### ✨ 优化
- 自动切号优先选择到期时间最近且额度最满的账号

### 🔧 修复
- 修复排序按钮失效（点击无响应）

---

## v5.9.1

### ✨ 新功能
- 设置面板新增「清除已过期账号」一键批量删除（精确到时分）
- 插件 ID 变动数据迁移：自动扫描所有旧版本数据源合并去重，不丢失账号（仅执行一次，不影响手动删除）

### 🔧 修复
- 到期时间精确到小时分钟（如"到期 4月7日 16:00"）
- 检查更新按钮旋转动画不再带动方框旋转
- 设置面板点击外部区域自动收起
- 移除「清除已耗尽账号」功能

---

## v5.9.0

### ✨ 新功能
- 多页面 UI：新增工具箱页面，扳手按钮一键切换主页/工具页
- 提示词收藏：新建、编辑、删除、点击即复制，支持 JSON 导入/导出（团队共享）
- 工具页包含：分身管理、提示词收藏、补丁工具、机器码管理
- 设置按钮移至当前账号区域右上角（工业风设计）
- 页面切换动画 + 折叠状态持久化

### 🔧 修复
- 修复 macOS 分身创建后无窗口打开的问题（改用 `open -n -a` 启动 .app bundle）
- 优化 Linux 进程检测命令，过滤 Electron helper 进程避免缓存错误路径
- 补充 Linux 常见安装路径（`/usr/bin/windsurf`、`/opt/Windsurf/windsurf`）

### 🧹 清理
- 移除死代码：Checkpoint、License 系统、MCP 对话、Codemap 等遗留模块

---

## v5.8.1

### ✨ 新功能
- 当前账号区域显示日刷新倒计时（距 GMT+8 16:00 剩余时间）
- 库存账号列表 mini 时间线也显示剩余周刷新次数

### 🔧 修复
- 修复试用期时间线周刷新次数显示错误：进度条已过的刷新点不再计入
- 修复 Linux 系统切换账号提示"没有文件写入权限"的问题，新增 pkexec 提权支持
- 修复 Linux 系统备份文件创建失败（目录无写入权限）的问题
- Firebase 登录添加自动重试（最多3次），解决网络瞬时故障导致切换失败的问题

---

## v5.8.0

### ✨ 新功能
- 全新原神风格 UI：配额血条、试用期时间线、徽章、按钮全面换装
- 快捷按钮配色区分：导入(金)、导出(青)、刷新(绿)，视觉辨识度更高
- 当前账号区域 + 标题栏/排序栏固定不滚动，账号列表独立滚动
- 默认启用全量配额定时刷新（每10分钟），不再依赖自动切换开关
- 试用期时间线周日星标 hover 显示具体日期（周限额刷新：X月X日）

### 🔧 修复
- 修复导出按钮无反应（webview 缺少 password 字段）
- 修复查询余额通知仍显示积分制，改为额度制（日剩X% 周剩X%）
- 当前账号二级信息统一使用带颜色的 HTML 样式，与列表账号一致

---

## v5.7.8

### ✨ 新功能
- 批量删除账号：设置面板新增「选择删除」，QuickPick 多选删除
- 导入兼容咸鱼发货格式（卡号N：邮箱 密码），自动过滤卡密行
- 配额 0% 显示「燃尽了」斜体文案，严格仅在真正 0% 时触发
- 配额进度条分级配色（绿/黄/红/暗红/燃尽灰）

### 🔧 修复
- 顶部版本栏和底部卡密区固定不随滚动消失
- 「更换卡密」增加二次确认弹窗，防止误触清除
- 切换账号通知改为状态栏 3 秒自动消失，不再长时间停留

---

## v5.7.7

- 所有功能均需激活卡密后使用（防白嫖）
- 账号购买改为三店选择（闲鱼×2 + 链动小店）
- 移除卡密区购买链接

---

## v5.7.6

- 已激活用户显示「更换卡密」「解绑」按钮
- 未激活用户显示免费功能提示

---

## v5.7.5

- 移除空状态下的"导入账号"入口

---

## v5.7.4

- 修复版本号显示 `v?.?.?` 的问题
- 修复更新检测反复提示的问题
- 修复分身管理插件ID不匹配

---

## v5.7.3

- 版本号更新

---

## v5.7.2

### ✨ 新功能

- **Section 标题栏血条**：PRO/TRIAL 账号标题显示总日剩余和总周剩余百分比，带动态配色
- **状态栏靠右**：底部状态栏移至右侧显示

### 🔧 修复

- **密码含分号兼容**：修复密码中包含 `;` 字符时导入解析错误的问题
- **QuickPick 显示统一**：快速切换面板改为显示「日剩xx%·周剩xx%」
- **README 全面重写**：移除旧版 MCP 插件说明，更新为切号器功能介绍，开头加卸载旧版本提示
- **VSIX 瘦身**：移除过时的 README 图片，包体从 2.94 MB 缩至 538 KB

---

## v5.7.1

### 🔧 修复

- **密码含分号兼容**：修复密码中包含 `;` 字符时导入解析错误的问题，现在只按第一个分号切分
- **QuickPick 显示统一**：快速切换账号面板改为显示「日剩xx%·周剩xx%」与面板一致

---

## v5.7.0

### ✨ 新功能

- **版本号与更新按钮移至顶部**：版本号和检查更新按钮移到顶部导航栏，检查更新不再需要卡密
- **检查更新反馈优化**：点击更新按钮后显示旋转动画，完成后版本号显示 ✓ 或 ⬆，底部状态栏同步显示结果
- **设置持久化**：自动切换、静默切换、阈值等设置在 package.json 中声明，重启 Windsurf 后不再丢失
- **UI 字体设计升级**：版本号、Section 标题、账号 Badge、配额数字、到期日期、设置标题等关键元素全面美化
- **配额数字同色显示**：进度条旁的百分比数字和标签与进度条颜色一致（绿/黄/红）
- **到期日期格式优化**：改为中文格式「到期 4月3日」，已过期显示删除线

### 🔧 修复

- **底部状态栏显示统一**：改为显示「日剩xx%·周剩xx%」与面板一致，不再反向显示已用百分比
- **卡密授权 UI 移入工具区**：卡密激活区域移入工具 section，简化底部区域

---

## v5.6.30

### ✨ 新功能

- **分身自定义名字**：新建分身时弹出输入框填写名称（如"公司服务器A"），列表中每个分身旁增加 ✏️ 重命名按钮
- **账号导出**：账号导入区域新增「导出」按钮，一键将所有账号以 邮箱;密码 格式复制到剪贴板，方便在 SSH 远程服务器粘贴导入

### 🔧 修复

- **webview prompt() 失效**：修复 VS Code webview 中 prompt() 被禁止导致新建/重命名分身无反应的问题，改用原生 showInputBox

---

## v5.6.29

### ✨ 新功能

- **账号列表排序**：Pro/Trial 分区头部新增排序按钮，支持按余额↑↓ / 到期时间↑↓ 排序，解决账号多时难以快速找到目标账号的问题

---

## v5.6.28

### ✨ 新功能

- **分身功能卡密门控**：分身（多实例）功能需要激活卡密后才能使用，未激活时显示锁定提示
- **MCP 配置管理面板**：端口状态旁新增 ⚙ 按钮，点击展开配置管理面板：
  - 勾选框：是否启动时自动配置 MCP + Global Rules
  - 一键清除配置：清除本插件写入的 mcp_config.json 和 global_rules.md
  - 一键手动配置：立即写入 MCP 配置和 Global Rules
  - 关闭自动配置后，插件只提供切号功能，不写入任何配置文件

### 🔧 修复

- **跨平台兼容性**：instanceManager 添加 macOS/Linux 进程检测（ps aux / pgrep / which）、macOS 双可执行文件路径支持
- **代码优化**：合并 launchInstance 重复的 win32/else 分支，extensionsDir 检测优先使用 vscode.extensions API（不依赖硬编码路径）

---

## v5.6.22

### 🔧 修复

- **修复重启后登录状态丢失**：移除机器码重置时对 `state.vscdb` 和 `storage.json` 的只读保护（`0o444`），该机制会阻断 Windsurf 的 auth session 写入，导致重启后概率性显示未登录
- **清理启动解锁逻辑**：移除不再需要的 `unlockStateDb` / `unlockStorageJsonFiles` 方法，消除启动时序竞态风险

---

## v5.6.20

### ✨ 新功能

- **图片缩略图预览**：粘贴/选择/拖拽图片后显示 80×60 缩略图方块，不再仅显示文件路径
- **图片放大查看**：点击缩略图弹出全屏预览遮罩，点击任意位置关闭

### 🔧 修复

- **自动换号默认关闭**：`freeAccountMonitorEnabled` 默认值从 `true` 改为 `false`，新安装用户不会自动开启换号监测
- **防止账号误删**：自动换号成功后不再删除旧账号，保留供用户手动管理
- **描述文字优化**：移除 "Free账号" 专属描述，改为通用 "账号" 描述

### ✨ 改进

- **MCP端口扩展**：可用端口从 5 个增加到 10 个（3200-3209），支持更多并发实例

---

## v5.6.17

### ✨ 改进

- **自动换号通用化**：换号功能不再限制 Free 账号，Pro/Trial 账号余额不足时也会自动切换到余额最高的可用账号
- **Settings 精简**：移除 3 个内测配置项（autoContinueUi / collab / askSmartFriend），减少用户困惑
- **Portable 精简**：可移植模式从 10 个配置项精简为 3 个（globalRulesPath / mcpConfigPath / mcpServerName），降低配置门槛

### 🗑️ 移除

- 移除协作组会（Collab）功能及全部前后端代码（~500行）
- 移除 Auto-Continue UI 开关和智能提问按钮的内测入口
- 移除 Portable 模式的 `enabled` 开关及 7 个冗余字段（codeiumProductDirName / globalRulesDir / globalRulesFileName / mcpServersKey / mcpServerKeyTemplate / mcpServerKeyRegex / mcpServerUrlKey / mcpServerExtraFields / mcpServerUrlTemplate）

---

## v5.6.15

### 🔧 修复

- **修复 MCP 自动连接失败**：服务器先启动再异步扫描端口，消除启动延迟；抢占防护增加 5 秒过期窗口，Windsurf 慢启动不再被误拦
- **移除废弃的狂点重连检测**：精简连接防护为抢占防护 + 端口隔离两层

---

## v5.6.14

### 🔧 修复

- **修复 feedbackInput 初始化报错**：switch-case 作用域共享导致 `const feedbackInput` 遮蔽外层变量，触发暂时性死区（TDZ）错误

---

## v5.6.13

### ✨ 新功能

- **机器码重置**：支持一键重置设备指纹（deviceFingerprint + installationId + telemetry IDs），切号时自动无感重置
- **切号自动重置**：手动切号和 Free 自动换号成功后，后台自动重置机器码，无需手动操作

### 🔧 修复

- **修复扩展路径检测失败**：新增 vscode.extensions API 和 vscode.env.appRoot 两种检测方式，解决非标准安装路径下找不到 extension.js 的问题
- **修复 sql.js 运行时加载失败**：sql.js 改为从 lib/ 目录独立加载，避免 webpack 打包破坏模块导出
- **修复日志白名单遗漏**：机器码重置相关日志加入白名单，用户可在输出面板查看重置结果

---

## v5.6.12

### 🔧 修复

- **修复激活403错误提示不明确**：云端返回业务拒绝（卡密已绑定等）时直接显示具体错误消息，不再笼统提示"请检查网络连接"
- **修复账号管理多账号同时高亮**：多窗口场景下 merge 后可能产生多个 isActive 账号，新增统一去重防护

### ✨ 新功能

- **待发队列按工作区隔离**：待发送队列改为通过扩展端磁盘持久化存储（与会话记录一致），不同工作区互不干扰

---

## v5.6.11

### 🔧 修复

- **修复插件启动UI冻结问题**：内联脚本中 `\n` 在外层模板字符串内被解释为真实换行，导致JS语法错误，UI无法交互
- **修复会话记录导出换行符**：导出历史和复制全部功能的换行符改用安全编码
- **优化混淆器配置**：禁用 stringArray 防止破坏 webview 模板字符串内容

### ✨ 新功能

- **会话记录导出**：新增"导出"按钮，一键复制全部会话记录到剪贴板
- **检查点重命名**：支持对代码检查点进行内联重命名
- **使用指南图标优化**：按钮图标统一使用 SVG 风格

---

## v5.6.9

### 🔧 修复

- **修复重启后机器码漂移问题**：机器码首次生成后持久化缓存，重启不再因 CPU/内存信息波动导致"卡密已绑定其他机器"
- **激活窗口期延长至7天**：卡密生成后7天内均可激活（原为1小时）
- **优化发货信息**：移除激活窗口提示，简化用户体验

---

## v5.6.8

### 🔧 修复

- **修复机器码冲突 Bug**：增强机器码唯一性（新增 hostname/homedir/CPU型号/内存大小），防止不同用户生成相同机器码
- **修复试用卡顶掉永久卡问题**：云端新增卡密优先级检查（永久 > 限时 > 试用），禁止低优先级卡密覆盖高优先级
- **支持旧用户自动迁移**：升级新版本后自动迁移旧机器码，不影响已激活状态

---

## v5.6.7

### 🔧 修复

- 修复“保存保底”在部分版本 workbench.desktop.main.js 含 ESM 语法（如 export/import）时被误判为语法异常，导致拒绝保存的问题

---

## v5.6.6

### 🔧 修复

- 修复部分用户在未激活时云端同步导致误显示“永久/已激活”的问题（本地无卡密时跳过 syncFromCloud）

---

## v5.6.5

### ✨ 新功能

- 会话历史新增复制按钮：支持复制 AI 摘要/用户输入
- 当前待回复摘要面板新增右下角“复制”按钮（复制原始摘要 Markdown）

### 🔧 修复

- 修复摘要面板在“无会话清空”场景下被误清空导致后续不显示的问题
- 优化摘要复制按钮布局（嵌入摘要面板内，避免被渲染覆盖）

---

## v5.6.4

### ✨ 新功能

- **实时显示当前账号余额（默认开启）**：顶部 `💰` 显示当前活跃账号余额并定时刷新
- **配置项新增**：支持开关 `wfDialog.activeAccountBalanceDisplayEnabled` 与刷新间隔 `wfDialog.activeAccountBalanceRefreshIntervalSeconds`

### 🔧 修复

- 修复 Free 自动监测关闭时仍会启动预热/检查的逻辑
- 修复 Free 监测状态文案不随阈值变化、关闭时文案误导的问题
- 修复 Settings 中修改开关/阈值/间隔后，插件内状态不会立即同步的问题

---

## v5.4.9

### 🔧 修复

- **修复待回复列表失效会话不清理问题**：通过 JSON-RPC id 精确关联，AI 断开时只清理对应会话
- **修复会话清理后 UI 自动切换**：被移除的会话自动切换到下一个，所有会话清空后重置状态
- **修复 updateParams 时更新 currentRequestId**：会话切换后 UI 正确高亮当前会话
- **修复清理所有会话后选项区域未清空**：清空选项并重置状态指示灯
- **修复 TDZ 卡死问题**：主题持久化到 globalState，防止 Webview 回收后丢失
- **修复主题覆盖 bug**：applyTheme 使用 classList 操作，保留全屏等状态类
- **优化历史渲染性能**：显示全部记录，使用 DocumentFragment + requestIdleCallback 分批渲染

---

## v5.4.2

### 🔧 修复

- 修复点击外部关闭收藏面板时全屏状态未正确清除的问题

---

## v5.4.1

### 🔧 修复

- 修复在输入框选中文字拖动时触发全局拖拽提示的问题

---

## v5.4.0

### ✨ 新功能

#### ⭐ 提示词收藏增强
- **编辑收藏**：点击 ✏️ 按钮可编辑已有的收藏
- **批量导入**：在内容框粘贴多条（用 `---` 分隔），点击"批量"按钮一键导入
- **选择性导出**：勾选要导出的收藏，支持 JSON/文本两种格式
- **TXT 格式导入**：支持从纯文本文件导入收藏

### 🔧 修复

- 修复勾选框点击时触发内容插入的问题
- 修复收藏面板全屏恢复失败的问题

---

## v5.2.6

### ✨ 新功能

#### 🔄 竞速请求
- 余额查询同时发起反代+直连请求，谁快用谁
- 兼容有魔法和无魔法的网络环境

#### 📅 订阅到期时间
- 查询余额时显示订阅到期日期
- 显示在额度旁边：💰 75 额度 · 到期: 2026/01/23

### 🔧 优化

#### 🛠️ 自适应补丁
- 使用正则表达式匹配多种 Windsurf 版本
- 兼容性更强，减少"版本不兼容"错误

---

## v5.2.5

### ✨ 新功能

#### 🌐 免魔法支持
- 账号查询、切换功能现在支持免魔法使用
- 通过 Cloudflare Worker 反代 Firebase/Windsurf API
- 自动降级：反代失败时自动切换到直连

---

## v5.2.4

### ✨ 新功能

#### 👤 账号切换功能
- 新增账号管理面板，支持批量导入账号
- 支持一键切换 Windsurf 账号（需应用补丁）
- 支持查询账号余额和订阅状态
- 支持重置机器码

### 🔧 优化
- 性能优化：将阻塞的 execSync 改为异步 execAsync
- UI 优化：账号管理按钮样式统一，支持隐藏
- 检测更新按钮添加图标

### 🐛 修复
- 修复 RegisterUser API 调用失败问题（两步认证+Protobuf）

---

## v5.2.3

### ✨ 新功能

#### 🌲 文件列表树状显示
- 工作区按钮文件列表改为树状结构显示
- 检查点文件设置改为带复选框的树状显示
- @ 提及文件列表工作区列改为树状显示
- 支持文件夹展开/收起交互

#### 🔧 多工作区支持
- 检查点现在会扫描所有工作区文件夹
- Diff 对比功能支持在所有工作区中查找文件
- 相对路径正确计算，从工作区根目录开始

#### 🔄 热更新支持
- 版本发布页面打开时自动加载上次发布的内容
- 支持版本号、下载链接、文件大小、更新日志的自动填充

### 🔧 修复
- 修复密码验证状态检测逻辑
- 修复版本发布功能"请先重新验证密码"的问题

---

## v5.2.0

### ✨ 新功能
- 多开修复
- 队列拖拽排序
- 自动连续提交
- 收藏面板全屏
- 界面配色统一

---

## v5.0.0

### ✨ 新功能

#### 📁 代码检查点增强
- 检查点批量删除：新增全选(☑)和批量删除(🗑️)按钮
- 每个检查点项添加复选框，支持多选删除
- 操作按钮移至文件预览前，展开后先显示按钮

#### ⚙️ 按钮配置面板
- 双击⚙️设置按钮打开按钮配置面板
- 自定义显示/隐藏功能按钮
- 配置持久化存储在 localStorage

#### 🎨 UI优化
- 按钮点击动效（:hover 和 :active 状态）
- 检查点面板高度限制（max-height: 45vh），避免覆盖输入框
- 文件差异预览高度减小（100px），按钮更易访问
- 窄面板适配：标题行 flex-wrap 换行

### 🔧 修复
- 检查点添加文件时的 1MB 大小限制提示
- placeholder 更新：双击🔔改音效 | 双击⚙️隐藏按钮
- **检查点/文件设置面板高度可调节**：支持拖拽右下角自由调节面板高度
- **修复 @ 提及文件列表不展开**：修复变量声明顺序导致的 TDZ 问题
- **@ 提及菜单显示更多/收起**：默认显示 10 个，点击“显示更多”展开全部

---

## v4.0.2

### ✨ 新功能

#### ⭐ 提示词收藏
- 点击⭐按钮打开收藏面板
- 添加/删除常用提示词模板
- 点击收藏项快速插入到输入框
- globalState持久化存储（插件更新不丢失）

#### 🎨 主题切换
- 新增"终端主题"：跟随IDE配色 + 去掉动画/emoji，适合低调使用
- 三主题循环切换：跟随IDE → 科技 → 终端

#### 📂 工作区选择器优化
- 两列布局：左侧"已打开"，右侧"工作区"
- 悬浮显示完整路径

### 🔧 UI优化

- ⭐收藏按钮移到⚙️齿轮旁边
- placeholder提示：`@提及文件 | ⭐收藏提示词 | Ctrl+V粘贴图片`
- 内容输入框宽度/高度优化

---

## v4.0.1

### 🐛 Bug 修复

#### 🔐 SSH 远程环境激活问题
- 修复 Remote SSH 环境下激活信息丢失的问题
- 添加 `extensionKind: ["ui", "workspace"]`，优先在本地运行扩展
- 激活信息始终存储在本地，不受远程机器指纹变化影响

### ✨ 新功能

#### 📂 文件选择器
- 新增"已打开"按钮，快速选择已打开的文件和工作区文件
- 分组显示：已打开文件在前，工作区文件在后
- 点击文件名添加到附件，向上展开选择器
- 悬浮显示完整文件路径

#### 🔍 @ 提及功能
- 输入 `@` 自动弹出文件列表
- 支持模糊搜索筛选
- 键盘导航（↑↓选择，Enter确认，Esc关闭）
- 选择后自动插入引用并添加到附件

#### ↩ 撤销/前进按钮
- 已打开文件后显示"撤销"和"前进"按钮
- 撤销：执行 Ctrl+Z 效果
- 前进：执行 Ctrl+Y 效果

#### 📍 代码引用功能
- 选中代码后显示 `💬 发送到插件 Ctrl+Shift+R` 提示
- 按 `Ctrl+Shift+R` 将选区引用添加到输入框
- 引用格式：`@文件名#L起始行-结束行`
- 支持右键菜单和编辑器标题栏图标

---

## v4.0.0

### ✨ 新功能

**1. 建议工具双击锁定**
- 单击按钮：临时选择，提交后自动取消
- 双击按钮：持久锁定（显示 🔒），提交后保持勾选

**2. 开头语更新亮灯提示**
- 云端开头语更新时，复制开头语按钮显示呼吸动画

### 🐛 Bug 修复

- 移除 AI 预填内容
- 保护用户输入
- 禁用自动路径识别
- 文件标签换行修复

---

## 下载

- [wf-dialog-mcp-5.0.0.vsix](./wf-dialog-mcp-5.0.0.vsix)
